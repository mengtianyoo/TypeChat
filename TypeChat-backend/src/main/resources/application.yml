server:
  port: 9999
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/typetest?useUnicode=true&useSSL=false&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 200209
  servlet:
    multipart:
      enabled: true
      max-file-size: 1024MB  #??????
      max-request-size: 1024MB

# ????
mybatis-plus:
  mapper-locations: classpath:/mybatis/*.xml
    #configuration:
  #log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
# ??????
path:
  image: /ddchat/image/
  video: /ddchat/video/
  audio: /ddchat/audio/
  file: /ddchat/file/
